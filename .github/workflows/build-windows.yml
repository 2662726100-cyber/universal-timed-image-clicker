name: Build Windows executable

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'
          cache: pip

      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check -r requirements-dev.txt

      - name: Run tests
        run: python -m unittest discover -s tests -v

      - name: Build executable
        shell: pwsh
        run: |
          pyinstaller --noconfirm --clean --onefile --windowed --name "通用定时图像点击器" --distpath dist --workpath build --specpath . --collect-all cv2 src/universal_clicker.py
          Copy-Item docs/通用版使用说明.md dist/通用版使用说明.md
          Get-FileHash -Algorithm SHA256 dist/通用定时图像点击器.exe | Format-List

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: 通用定时图像点击器-Windows
          path: |
            dist/通用定时图像点击器.exe
            dist/通用版使用说明.md
          if-no-files-found: error

      - name: Create GitHub Release for tag
        if: startsWith(github.ref, 'refs/tags/v')
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${{ github.ref_name }}" "dist/通用定时图像点击器.exe" "dist/通用版使用说明.md" --generate-notes --title "通用定时图像点击器 ${{ github.ref_name }}"
